<%= form_for @post do |f| %>
	<%= f.label :title%>
	<%= f.text_field :title %>
	
	<%=f.label :description %>
	<%=f.text_field :description %>

	<%= f.label :picture %>
	<%= f.file_field :picture %>

	<%= f.submit "Post it!"%>

	<%end%>