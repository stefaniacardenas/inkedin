<%= form_for @post do |f| %>
	<%= f.label :title%>
	<%= f.text_field :title %>

	<%= f.label :picture %>
	<%= f.file_field :picture %>
	
	<%=f.label :description %>
	<%=f.text_field :description %>

	<%= f.submit "Post it!"%>

	<%end%>